
// You can copy this file and rename as 'config.h' and  use it to store the config
// details related your project, which are private, and should not visible in the git history.
// By default, the config.h is ignored from the git

#ifndef CONFIG_H
#define CONFIG_H

// WiFi Credentials -------
#define WIFI_SSID ""
#define WIFI_PASSWORD ""

#endif
